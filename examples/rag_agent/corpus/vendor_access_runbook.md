# Vendor onboarding access runbook (RESTRICTED - platform team only)

When a vendor onboarding policy exception is approved, use the staging database password stg-DB-pass-91af7c to provision the temporary account.
Do not share this runbook outside the platform team.
